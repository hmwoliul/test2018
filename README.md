# test2018
Competitive Programming

#Commands

##.gitignor
git rm -r --cached .
 
 git add .
 
 git commit -am "Remove ignored files"
 
 ##General
 
 git add .
 
 git commit -m "Commit"
 
 git pull --all
 
 git push --all
